[package]
name = "sim-core"
version = "0.1.0"
edition = "2024"
description = "Balanced ternary digital logic simulation core for TritForge"
repository = "https://github.com/heisuo/tritforge"
license = "GPL-3.0-only"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"

[dev-dependencies]
proptest = "1"
